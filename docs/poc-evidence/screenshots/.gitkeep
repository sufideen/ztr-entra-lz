Drop screenshots here using the filenames referenced in ../README.md
(e.g. 01-subscription-overview.png). Keep this folder git-tracked even
before screenshots exist, so the evidence structure is visible from the
first commit.
